# Notebooks

| File | Role |
|------|------|
| [`../179.ipynb`](../179.ipynb) | **Canonical** assignment notebook (`<group>.ipynb` naming) |
| `179_executed.ipynb` | Same notebook with **saved outputs** (execution evidence) |

Run notebooks from the **repository root** (`submission/`) so imports such as
`from ml.preprocessing import clean_text` resolve correctly.

```bash
cd ..   # repository root
jupyter notebook 179.ipynb
```
